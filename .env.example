# PTR v2 — environment templates index
#
# | Template | Purpose |
# | ---------- | --------- |
# | [.env.development.example](.env.development.example) | Local development: ptr_coder (LM Studio), Docker Compose |
# | [.env.production.example](.env.production.example) | Production-shaped variable names; inject secrets via platform |
#
# Copy the needed example to a gitignored file (e.g. `.env.development`) or bind into your orchestrator — never commit real secrets.

# ptr_coder (ADR-0006): full defaults live in `.env.development.example`.
# PTR_CODER_BASE_URL=http://localhost:1234/v1
# PTR_CODER_MODEL=qwen3-coder-30b-a3b-instruct
