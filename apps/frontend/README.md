# Frontend (scaffold)

Placeholder build context for the future Vite + React workspace. Compose maps host port `FRONTEND_PUBLISH_PORT` (default `15173`, container listens on `5173`) per `docker-compose.yml`.
